
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Masukkan Kode Akses untuk <%= praktikum.nama_praktikum %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      /* Tambahkan styling kustom di sini jika diperlukan */
    </style>
  </head>
  <body class="bg-gradient-to-br from-purple-700 to-purple-500 font-sans flex items-center justify-center min-h-screen">

    <div class="bg-white p-8 rounded-xl shadow-lg max-w-md w-full text-center">
      <h2 class="text-3xl font-bold mb-6 text-purple-800">Masukkan Kode Akses</h2>
      <p class="text-lg text-gray-700 mb-6">Untuk Praktikum: <span class="font-semibold"><%= praktikum.nama_praktikum %></span></p>

      <% if (error) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
          <strong class="font-bold">Oops!</strong>
          <span class="block sm:inline"><%= error %></span>
        </div>
      <% } %>

      <form action="/pilihLab/joinLab" method="POST" class="space-y-4">
        <input type="hidden" name="labId" value="<%= praktikum.id %>" />
        <div>
          <label for="labCode" class="sr-only">Kode Akses</label>
          <input
            type="password" id="labCode"
            name="labCode"
            class="mt-1 block w-full px-4 py-2 border border-purple-400 rounded-md shadow-sm placeholder-gray-500 focus:outline-none focus:ring-purple-500 focus:border-purple-500 sm:text-sm text-gray-700"
            placeholder="Masukkan Kode Akses"
            required
          />
        </div>
        <button
          type="submit"
          class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-purple-700 hover:bg-purple-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500"
        >
          Gabung Praktikum
        </button>
      </form>

      <a href="/pilihLab" class="block text-purple-700 hover:underline mt-6 text-sm">Kembali ke Daftar Lab</a>
    </div>

  </body>
</html>