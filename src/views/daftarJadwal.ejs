<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Jadwal - <%= currentLab.nama_praktikum %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <div class="flex min-h-screen">
        <%- include('sidebar', { currentPage: currentPage }) %>

        <main class="flex-1 p-8">
            <h2 class="text-3xl font-bold mb-6 text-purple-800">Jadwal Praktikum: <%= currentLab.nama_praktikum %></h2>

            <% if (locals.message && locals.message.text) { %>
                <div class="mb-4 p-3 rounded-md 
                    <% if (message.type === 'success') { %> bg-green-100 text-green-700 
                    <% } else if (message.type === 'error') { %> bg-red-100 text-red-700 
                    <% } else { %> bg-yellow-100 text-yellow-700 <% } %>">
                    <%= message.text %>
                </div>
            <% } %>

            <% if (daftarJadwal.length === 0) { %>
                <p class="text-gray-600">Belum ada jadwal yang tersedia untuk praktikum ini.</p>
            <% } else { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% daftarJadwal.forEach((jadwal, index) => { %>
                        <div class="bg-white p-6 rounded-lg shadow-md">
                            <h3 class="text-xl font-semibold text-purple-700 mb-2">Pertemuan <%= index + 1 %></h3>
                            <p class="text-gray-600 mb-2"><span class="font-semibold">Hari/Tanggal:</span> <%= jadwal.tanggal_formatted %></p>
                            <p class="text-gray-600 mb-4"><span class="font-semibold">Pukul:</span> <%= jadwal.jam_formatted %></p>
                            <a href="/jadwal/<%= jadwal.id %>" class="inline-block bg-purple-600 text-white px-4 py-2 rounded-md hover:bg-purple-700 transition">Lihat Detail</a>
                        </div>
                    <% }) %>
                </div>
            <% } %>
            <div class="mt-8 text-center">
                <a href="/dashboard" class="text-purple-600 hover:underline">Kembali ke Dashboard Praktikum</a>
            </div>
        </main>
    </div>
</body>
</html>