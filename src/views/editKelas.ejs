<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Edit Kelas - LABCORNER</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-yellow-100 to-yellow-300 min-h-screen flex">

    <aside class="w-64 bg-gray-800 text-white p-6 rounded-r-2xl flex flex-col">
        <h1 class="text-2xl font-bold mb-10">LABCORNER</h1>
        <nav class="space-y-4">
            <a href="/lab" class="flex items-center text-gray-300 hover:text-white transition-colors duration-200">
                Home Lab
            </a>
            <a href="/kelas/tambah" class="flex items-center text-gray-300 hover:text-white transition-colors duration-200">
                Tambah Kelas
            </a>
        </nav>
    </aside>

    <main class="flex-1 flex justify-center items-center p-6">
        <form action="/kelas/<%= praktikum.id %>/update" method="POST" class="bg-white p-10 rounded-xl shadow-lg w-full max-w-xl">
            <h2 class="text-2xl font-bold text-center mb-8">Edit Kelas</h2>

            <div class="space-y-5">
                <div>
                    <label class="block text-gray-700 font-semibold mb-1">Nama Praktikum</label>
                    <input type="text" name="nama_praktikum" value="<%= praktikum.nama_praktikum %>"
                        class="w-full border rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-yellow-400" required />
                </div>

                <div>
                    <label class="block text-gray-700 font-semibold mb-1">Lab</label>
                    <select name="lab_id" class="w-full border rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
                        <% labs.forEach(lab => { %>
                            <option value="<%= lab.id %>" <%= praktikum.lab_id === lab.id ? 'selected' : '' %>>
                                <%= lab.nama_lab %>
                            </option>
                        <% }) %>
                    </select>
                </div>

                <div>
                    <label class="block text-gray-700 font-semibold mb-1">Kode Masuk</label>
                    <input type="text" name="kode_masuk" value="<%= praktikum.kode_masuk %>"
                        class="w-full border rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-yellow-400" required />
                </div>
            </div>

            <div class="flex justify-between mt-8">
                <a href="/lab" class="bg-gray-500 text-white px-6 py-2 rounded-md hover:bg-gray-600">Batal</a>
                <button type="submit" class="bg-green-500 text-white px-6 py-2 rounded-md hover:bg-green-600">Simpan Perubahan</button>
            </div>
        </form>
    </main>
</body>
</html> 