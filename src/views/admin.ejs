<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dasbor Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet"/>
  <style>
    body { font-family: 'Poppins', sans-serif; }
  </style>
</head>
<body class="bg-gray-100 min-h-screen">
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-72 bg-gray-800 text-white flex flex-col p-4">
      <div class="text-2xl font-bold tracking-wide mb-8">LABCORNER (Admin)</div>
      <nav class="flex flex-col gap-4">
        <a href="/admin/dashboard" class="flex items-center gap-3 p-3 bg-gray-700 rounded-lg font-semibold">
          <span>Dasbor</span>
        </a>
        <a href="/mahasiswa/rekap" class="flex items-center gap-3 p-3 text-gray-300 hover:bg-gray-700 rounded-lg">
          <span>Rekap Mahasiswa</span>
        </a>
        <!-- Tambahkan menu admin lainnya di sini -->
      </nav>
      <div class="mt-auto">
        <a href="/logout" class="flex items-center gap-3 p-3 text-red-400 hover:bg-red-500 hover:text-white rounded-lg">
          <span>Logout</span>
        </a>
      </div>
    </aside>

    <!-- Main Content -->
    <div class="flex-1 flex flex-col overflow-y-auto">
      <header class="flex justify-between items-center px-6 py-4 bg-white shadow-md">
        <h1 class="text-xl font-bold text-gray-800">Dasbor Admin</h1>
        <div class="text-sm text-gray-600">
          Selamat datang, <%= user?.username %>
        </div>
      </header>

      <main class="p-8 space-y-6">
        <div class="bg-white p-6 rounded-xl shadow-xl">
          <h2 class="text-xl font-semibold text-gray-800 mb-4">Selamat Datang di Dasbor Admin</h2>
          <p class="text-gray-600">
            Anda dapat mengelola laboratorium, praktikum, dan data mahasiswa dari sini.
          </p>
        </div>
      </main>
    </div>
  </div>
</body>
</html> 